Most of this repo at the moment is actually just random research in [`NOTES.md`](NOTES.md)

building:

```
npm install
npm run lint
npm run build
```

Alternatively, `gulp watch` seems to work for continuous builds
